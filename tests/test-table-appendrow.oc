table<int, string, float> t = read("tests/sampleTable.csv", ["int", "string", "float"], ",");

tuple<int, string, float> tup =  (4, "d", 4.0);

t = t.appendRow(tup);

write("test-table.appendrow.out", t, ",");
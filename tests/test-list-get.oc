list<int> l = [10,20,30,40];
int i = 0;
while(i < l.list_length()){
    int elem = l.list_get(i);
    print("(%d, %d), ", i, elem);
    i = i + 1;
}

list<float> f = [3.14, 6.28, 2419.948, 84.000011, 0.0];
i = 0;
while(i < f.list_length()){
    float elem = f.list_get(i);
    print("(%d, %f), ", i, elem);
    i = i + 1;
}

list<string> s = ["Sphinx", "of", "black", "quartz", "judge", "my", "vow"];
i = 0;
while(i < s.list_length()){
    string elem = s.list_get(i);
    print("(%d, %s), ", i, elem);
    i = i + 1;
}

list<bool> b = [true, false, true, true];
i = 0;
while(i < b.list_length()){
    bool elem = b.list_get(i);
    print("(%d, %d), ", i, elem);
    i = i + 1;
}

list<int> garr = [1, 2, 3, 4, 5, 6, 7];
fn test_function (list<int> arr) -> list<int> {
    list<int> arr1 = arr.replace(2, 31);
    list<int> arr2 = arr1.replace(3, 41);
    return arr2;
}

list<int> rvalue = test_function(garr);

int i = 0;
int length = garr.length();
while(i < length){
    print("%d,", garr.get(i));
    i = i + 1;
}
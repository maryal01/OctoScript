fn test-lambda (int z) -> int {
    int x = fn (int x, bool y) => int {
                if (y) then {
                    x + 1;
                }
                else {
                    x - 1;
                }
            };
    return x(z, true);    
}

print(test-lambda(3));
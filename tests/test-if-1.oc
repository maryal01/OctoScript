fn test-if (int i, bool b) -> int {
    if (b) {
        if ( i < 5) {
            return 2;
        }
        else {
            return i;
        }
    }
    else {
        return -i;
    }
}

print("%d", test-if(-10, true));
print("%d", test-if(10,  true));
print("%d", test-if(10, false));

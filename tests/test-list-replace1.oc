list<int> garr  = [1, 2, 3, 4, 5, 6, 7];
int i = 0;
int length = garr.length();
while(i < length){
    print("%d,", garr.get(i));
    i = i + 1;
}
print("\n");

garr.replace(3, 41);
i = 0;
while(i < length){
    print("%d,", garr.get(i));
    i = i + 1;
}
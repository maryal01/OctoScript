list<string> l1 = ["hari", "krishna", "ramayan"];
list<string> l2 = ["rooney", "tevez", "beckham", "adebayor"];
list<string> arr = l1.concat(l2);
sort(arr, fn (string a, string e) => { stringCmp(a,e) < 0  });

int l = arr.length();
int i = 0;
while(i < l){
    print(arr.get(i));
    print("\n");
    i = i + 1;
}